#ifndef ENUM_H
#define ENUM_H

//ordre top / front / left / right / back

enum FaceIndex { top = 0,front = 1,left = 2,right = 3, back = 4 };

enum FillOrient { horz, vert, horzinvert,vertinvert };

#endif // ENUM_H
